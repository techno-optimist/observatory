# Observatory MCP Server Configuration
# Copy to .env and customize for your environment

# =============================================================================
# Backend Connection
# =============================================================================

# URL of the Observatory backend API
# Local development:
OBSERVATORY_BACKEND_URL=http://127.0.0.1:8000

# Production (Render):
# OBSERVATORY_BACKEND_URL=https://observatory-backend.onrender.com

# =============================================================================
# Server Settings
# =============================================================================

# Port for the SSE server
PORT=8080

# Host to bind to (0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Default embedding model (used when client doesn't specify)
OBSERVATORY_DEFAULT_MODEL=all-MiniLM-L6-v2

# =============================================================================
# CORS Settings (optional)
# =============================================================================

# Comma-separated list of allowed origins
# Default: * (allow all)
# Production: restrict to known clients
# ALLOWED_ORIGINS=https://claude.ai,https://your-app.com
