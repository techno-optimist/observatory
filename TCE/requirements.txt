# TCE Server Dependencies (Render deployment)

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Tinker SDK (cloud model training & inference)
tinker
